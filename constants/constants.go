package constants

const FILE = "file"
const DEFAULT_CONFIG = "dev_local.json"
const CONFIG_FILE_USAGE = "File to read all configuration like mongo server"

const MONGO_BASE_URL = "mongodb://"

const EMPTY_TOKEN_MSG = "Token not present in the header - request forbidden"
const INVALID_TOKEN_MSG = "Invalid authorization token"
const INVALID_TOKEN_MSG_INCORRECT_ID = "Invalid authorization token - Does not match Employee ID"
const INVALID_TOKEN_MSG_TOKEN_EXPIRED = "Invalid authorization token - Token is either expired or no longer active"
const SUCCESS_TOKEN = "Success"

const USER_KEY = "employee"
